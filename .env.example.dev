# =============================================================================
# Development Environment — local machine
# =============================================================================
# Copy to .env and fill in your values.
# The data pipeline loads this via python-dotenv from the project root.

# ---------------------------------------------------------------------------
# LLM API Keys (required)
# ---------------------------------------------------------------------------
OPENROUTER_API_KEY=your-openrouter-api-key

# Google Cloud / Vertex AI (for Gemini models)
# Auth: run `gcloud auth application-default login`
GOOGLE_CLOUD_PROJECT=nof1-causal-lab
GOOGLE_CLOUD_LOCATION=global

# Exa API — literature search in Stage 4 (optional, disable in config.yaml)
#EXA_API_KEY=your-exa-api-key

# ---------------------------------------------------------------------------
# Storage — defaults to local filesystem (non-production), no config needed
# ---------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# Web App (copy to apps/web/.env.local for Next.js)
# ---------------------------------------------------------------------------
APP_SECRET=replace-with-32-plus-char-random-string
#BYOK_SECRET_STORE_URL=file:.local/byok-secret-store.db
#BYOK_SECRET_STORE_AUTH_TOKEN=your-turso-auth-token
#OPENROUTER_CREDITS_API_KEY=
# Server-side web routes use the same local defaults unless overridden:
#PREFECT_API_URL=http://localhost:4200/api
#TOOL_SERVER_URL=http://localhost:8100
# Browser-side Prefect event stream override (optional)
#NEXT_PUBLIC_PREFECT_EVENTS_URL=ws://localhost:4200/api/events/out
#NEXT_PUBLIC_MOCK_DATA=true

# ---------------------------------------------------------------------------
# JAX (optional)
# ---------------------------------------------------------------------------
#JAX_COMPILATION_CACHE_DIR=~/.cache/nof1-causal-lab/jax
#NOF1_CAUSAL_LAB_DISABLE_JAX_PERSISTENT_CACHE=false
