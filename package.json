{
  "name": "causal-ssm-agent",
  "private": true,
  "packageManager": "bun@1.2.23",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "prepare": "git config core.hooksPath .githooks",
    "dev": "turbo dev",
    "build": "turbo build",
    "cloc:readme": "bun scripts/update_readme_cloc.js",
    "docs:codegen": "bun run --cwd packages/api-types codegen && bun run cloc:readme && bun scripts/codegen_docs_latex.js",
    "docs:check": "cd apps/data-pipeline && uv run python scripts/export_distribution_docs.py --check && cd ../.. && bun run --cwd packages/api-types codegen:check && bun scripts/update_readme_cloc.js --check && bun scripts/codegen_docs_latex.js --check && markdownlint-cli2 --config .markdownlint-docs.json \"README.md\" \"docs/**/*.md\"",
    "integration:start": "bash scripts/start_agentic_integration_stack.sh",
    "lint": "turbo lint",
    "lint:fix": "turbo lint:fix",
    "test": "turbo test",
    "format": "turbo format",
    "type-check": "turbo type-check",
    "check": "turbo run check docs:check",
    "storybook": "turbo storybook"
  },
  "devDependencies": {
    "markdownlint-cli2": "^0.22.0",
    "mathjax-full": "3.2.2",
    "turbo": "^2"
  }
}
