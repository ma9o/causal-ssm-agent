{
  "name": "@causal-ssm/data-pipeline",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn causal_ssm_agent.tool_server:app --reload --port 8100",
    "test": "uv run pytest tests/ -m 'not slow'",
    "test:all": "uv run pytest tests/",
    "lint": "uv run ruff check src/ tests/ && uv run ruff format --check src/ tests/ && uv run python scripts/validate_config.py",
    "lint:fix": "uv run ruff check --fix src/ tests/ && uv run ruff format src/ tests/",
    "format": "uv run ruff format src/ tests/",
    "check": "bun run lint && bun run test",
    "type-check": "uv run ty check src/ tests/"
  }
}
