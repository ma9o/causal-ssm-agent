[project]
name = "nof1-causal-lab"
version = "0.1.0"
description = "LLM-orchestrated causal inference over long-context, multi-source data"
readme = "../../README.md"
license = "Apache-2.0"
authors = [
    { name = "ma9o" }
]
requires-python = ">=3.12"
dependencies = [
    "arviz>=0.19.0",
    "blackjax>=1.2.0",
    "cloudpickle>=3.1.1",
    "cuthbert>=0.0.10",
    "diffrax>=0.6.0",
    "equinox>=0.11.0",
    "exa-py>=1.0.0",
    "fastapi>=0.115.0",
    "fsspec>=2024.2.0",
    "jax>=0.4.35",
    "jaxlib>=0.4.35",
    "httpx>=0.27.0",
    "jaxtyping>=0.3.0",
    "mcp>=1.0.0",
    "openai>=2.20.0",
    "optimistix>=0.0.9",
    "networkx>=3.6",
    "numpy>=2.3.3",
    "numpyro>=0.16.0",
    "optax>=0.2.0",
    "polars>=1.35.2",
    "pydantic>=2.11.9",
    "pyyaml>=6.0.2",
    "python-dotenv>=1.2.1",
    "scikit-learn>=1.7.2",
    "y0 @ git+https://github.com/y0-causal-inference/y0.git",
    "ipython>=9.12.0",
    "sympy>=1.14.0",
    "temporalio>=1.30.0",
]

[project.scripts]
nof1-publish = "nof1_causal_lab.publish:main"

[build-system]
requires = ["uv_build>=0.8.24,<0.11.0"]
build-backend = "uv_build"

[dependency-groups]
cloud = [
    "modal>=1.3.2",
    "s3fs>=2024.2.0",
]
eval = [
    "inspect-ai>=0.3.162",
]
dev = [
    "beartype>=0.19.0",
    "ipykernel>=7.2.0",
    "ipywidgets>=8.1.7",
    "jupyter-client>=8.8.0",
    "marimo>=0.23.11",
    "nbconvert>=7.16.6",
    "nbformat>=5.10.4",
    "plotly>=6.7.0",
    # slim[openai], NOT full pydantic-ai: the full meta-package pulls every
    # provider extra, whose combined pins cap resolution at 1.38.0 — below
    # marimo's required pydantic-ai-slim[openai]>=1.107.0 (OpenAIChatCompatibleProvider).
    "pydantic-ai-slim[openai]>=1.107.0,<2.0.0",
    "pymc>=5.16",
    "pytest>=9.0.2",
    "pytest-instafail>=0.5.0",
    "pytest-timeout>=2.3.1",
    "pytest-xdist>=3.6.1",
    "ruff>=0.9.0",
    "ty>=0.0.51",
    "vulture>=2.14",
]

[tool.ruff]
target-version = "py312"
line-length = 100
src = ["src", "tests", "evaluation"]
fix = true
# notebooks/data holds study fixtures and their data generators, not source code
extend-exclude = ["notebooks/data"]

[tool.ruff.lint]
select = [
    "E",      # pycodestyle errors
    "W",      # pycodestyle warnings
    "F",      # Pyflakes
    "I",      # isort
    "B",      # flake8-bugbear
    "BLE",    # flake8-blind-except
    "C4",     # flake8-comprehensions
    "DTZ",    # flake8-datetimez
    "NPY",    # NumPy-specific rules
    "PERF",   # Perflint
    "PT",     # flake8-pytest-style
    "RET",    # flake8-return
    "S324",   # insecure hash functions
    "S608",   # string-built SQL
    "TID",    # flake8-tidy-imports
    "UP",     # pyupgrade
    "ARG",    # flake8-unused-arguments
    "SIM",    # flake8-simplify
    "TCH",    # flake8-type-checking
    "PTH",    # flake8-use-pathlib
    "RUF",    # Ruff-specific rules
]
ignore = [
    "E501",   # line too long (handled by formatter)
    "F722",   # jaxtyping shape annotations are strings ruff treats as forward refs
    "PERF401",# list/dict comprehension nudges are too noisy for legacy code
    "PERF403",# dict comprehension nudges are too noisy for legacy code
    "SIM108", # use ternary operator (not always clearer)
    "RUF001", # ambiguous unicode in strings (intentional for display)
    "RUF002", # ambiguous unicode in docstrings (intentional for scientific notation)
    "RUF003", # ambiguous unicode in comments (intentional for scientific notation)
    "RUF022", # __all__ not sorted (comments prevent auto-sort)
]

[tool.ruff.lint.per-file-ignores]
"tests/*.py" = [
    "ARG001",  # unused function arguments (pytest fixtures)
    "ARG002",  # unused method arguments (pytest fixtures)
    "DTZ001",  # tests intentionally use naive datetimes as compact fixtures
]
"tests/**/*.py" = [
    "ARG001",  # unused function arguments (pytest fixtures)
    "ARG002",  # unused method arguments (pytest fixtures)
    "DTZ001",  # tests intentionally use naive datetimes as compact fixtures
]
"notebooks/**/*.py" = [
    "C408",  # plotly figure kwargs idiomatically use dict(...), not {} literals
    "E731",  # one-line math lambdas (e.g. φ = a·b) read clearly in gallery cells
]

[tool.ruff.lint.flake8-pytest-style]
parametrize-names-type = "tuple"

[tool.ruff.lint.flake8-tidy-imports]
ban-relative-imports = "parents"

[tool.ruff.lint.isort]
known-first-party = ["nof1_causal_lab"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false

[tool.vulture]
paths = ["src", "tests", "evaluation", "scripts", "notebooks"]
# notebooks/data holds study fixtures and their data generators, not source code
exclude = ["*notebooks/data*"]
min_confidence = 60
sort_by_size = true
ignore_decorators = [
    "@task",
    "@flow",
    "@pytest.fixture",
    "@solver",
    "@scorer",
    "@app.*",
    "@router.*",
    "@*_router.*",
    "@register_*",
    "@field_validator",
    "@model_validator",
    "@validator",
    "@root_validator",
    "@computed_field",
    "@property",
    "@*.defjvp",
    "@*.defvjp",
    "@*.list_tools",
    "@*.call_tool",
    # MCP gateway tools are invoked by the MCP runtime, never by our code
    "@mcp.tool",
]
ignore_names = [
    "test_*",
    "model",
    "model_config",
    "pytestmark",
    "should_exit",
    "exc_type",
    "exc_val",
    "exc_tb",
    # marimo notebooks emit this module-level provenance stamp (used by marimo, not us)
    "__generated_with",
    # jax.profiler.ProfileOptions attributes (consumed by JAX, invisible to vulture)
    "host_tracer_level",
    "python_tracer_level",
    "include_dataset_ops",
    "enable_hlo_proto",
]

[tool.ty.environment]
python-version = "3.12"

[tool.ty.src]
include = ["src", "notebooks"]

[tool.ty.rules]
unused-type-ignore-comment = "ignore"

[tool.ty.analysis]
respect-type-ignore-comments = true
# `Any`-out only deps ty cannot check cleanly, for two distinct reasons:
#   - no py.typed (types not exported): blackjax, cuthbert, cuthbertlib, numpyro, optax.
#   - typed, but ty (pre-1.0) mis-models them: equinox (its Module metaclass /
#     dataclass_transform widens our subclass construction to `Module`) and arviz
#     (`from_dict(**kwargs)` splat). Revisit as ty matures.
# diffrax / optimistix / y0 ship py.typed and ty handles them — kept checked.
replace-imports-with-any = [
    "arviz.**",
    "blackjax.**",
    "cuthbert.**",
    "cuthbertlib.**",
    "equinox.**",
    "numpyro.**",
    "optax.**",
]

[tool.pytest.ini_options]
addopts = "-n auto --tb=short -ra --instafail --timeout=300 --durations=10 --jaxtyping-packages=nof1_causal_lab.models.ssm.covariance_utils,nof1_causal_lab.models.ssm.discretization.exact,nof1_causal_lab.models.ssm.discretization.local_linearization,nof1_causal_lab.models.ssm.dynamics.steady_state,nof1_causal_lab.models.ssm.dynamics.vector_field,nof1_causal_lab.models.ssm.inference.methods.marginal_particle_gibbs._math,nof1_causal_lab.models.ssm.inference.targets.base,nof1_causal_lab.models.ssm.inference.targets.emissions,nof1_causal_lab.models.ssm.inference.targets.euler_maruyama,nof1_causal_lab.models.ssm.inference.targets.observation_kernel_helpers,nof1_causal_lab.models.ssm.inference.targets.transitions,beartype.beartype"
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
]
