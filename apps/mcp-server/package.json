{
  "name": "@causal-ssm/mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "start": "bun run src/index.ts",
    "test": "bun test",
    "codegen": "bun run scripts/generate.ts",
    "codegen:check": "bun run codegen && git diff --exit-code src/generated/"
  },
  "dependencies": {
    "@causal-ssm/api-types": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "@causal-ssm/typescript-config": "workspace:*",
    "typescript": "^5"
  }
}
