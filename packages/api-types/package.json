{
  "name": "@nof1-causal-lab/api-types",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "files": [
    "src"
  ],
  "scripts": {
    "type-check": "tsc --noEmit",
    "lint": "biome check .",
    "lint:fix": "biome check --fix .",
    "format": "biome format --write .",
    "codegen:docs": "cd ../../apps/data-pipeline && uv run python scripts/export_distribution_docs.py",
    "codegen:schemas": "cd ../../apps/data-pipeline && uv run python scripts/export_schemas.py && cd ../../packages/api-types && bun run scripts/generate.ts",
    "codegen": "bun run codegen:docs && bun run codegen:schemas",
    "check": "bun run codegen:check && bun run lint && bun run type-check",
    "codegen:check": "cd ../../apps/data-pipeline && uv run python scripts/export_schemas.py --check && cd ../../packages/api-types && bun run scripts/generate.ts --check"
  },
  "devDependencies": {
    "@nof1-causal-lab/typescript-config": "workspace:*",
    "json-schema-to-typescript": "^15.0.4",
    "typescript": "^5"
  }
}
