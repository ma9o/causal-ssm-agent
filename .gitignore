__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
ENV/
.env
*.egg-info/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.log
.DS_Store

# Data files (raw and processed are gitignored, training and queries are tracked)
data/raw/**
data/processed/**

# Inspect AI eval logs
logs/

# Track queries and training data (committed)
# data/queries/ - test queries for pipeline

scratchpad/**

repomix-output.xml

# R/renv (project-local R library for ctsem parity tests)
# Track: renv.lock, renv/activate.R, renv/settings.json, .Rprofile
# Ignore: library and other generated directories
renv/library/
renv/local/
renv/cellar/
renv/lock/
renv/python/
renv/sandbox/
renv/staging/
renv/.installed
.Rhistory
.RData
.Rproj.user

# Streamlit component artifacts
lib/

# Always track gitkeeps (must be last to override above patterns)
!**/.gitkeep
